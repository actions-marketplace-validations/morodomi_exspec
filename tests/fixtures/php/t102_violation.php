<?php

class ComplexTest extends \PHPUnit\Framework\TestCase
{
    public function test_complex_setup($db, $cache, $logger, $mailer, $queue, $config, $auth): void
    {
        $this->assertTrue(true);
    }
}

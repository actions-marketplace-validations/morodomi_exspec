// No parameterized tests → ratio = 0 (< 0.1)
test('add one', () => {
  expect(1 + 1).toBe(2);
});

test('add two', () => {
  expect(2 + 2).toBe(4);
});

test('add three', () => {
  expect(3 + 3).toBe(6);
});

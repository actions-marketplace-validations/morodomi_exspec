[package]
name = "exspec-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "exspec"
path = "src/main.rs"

[dependencies]
exspec-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }

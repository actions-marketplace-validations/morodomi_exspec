[package]
name = "exspec-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde-sarif = { version = "0.8", features = ["opt-builder"] }
toml = "0.8"
tree-sitter = "0.24"
streaming-iterator = "0.1"
